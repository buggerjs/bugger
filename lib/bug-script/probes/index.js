// Generated by CoffeeScript 2.0.0-beta5
var knownProbes;
knownProbes = [
  './coffee',
  './network',
  './profiler'
];
module.exports = function (safe) {
  var compilers, scriptContext;
  if (null == safe)
    safe = false;
  compilers = {
    '.js': {
      compile: function (filename, code, cb) {
        return cb(null, {
          code: code,
          map: null
        });
      },
      compileString: function (input) {
        return input;
      }
    }
  };
  scriptContext = { compilers: compilers };
  knownProbes.forEach(function (probeModule) {
    var load;
    load = require(probeModule).load;
    return load(scriptContext, safe);
  });
  return scriptContext;
};
